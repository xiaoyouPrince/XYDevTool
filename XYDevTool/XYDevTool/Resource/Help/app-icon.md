# AppIcon 生成器

## 功能

从一张源图生成 **iOS / macOS** 应用所需的全套 AppIcon 尺寸，并导出到指定目录。

## 使用步骤

1. 主界面进入图标生成功能
2. 选择源图片（建议正方形、足够分辨率）
3. 选择目标平台（iOS / macOS）
4. 指定导出目录，一键生成

## 说明

- 本项目图标即由该功能生成
- 导出后可将 `AppIcon.appiconset` 拖入 Xcode 资源目录
