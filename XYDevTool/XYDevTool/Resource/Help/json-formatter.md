# JSON 格式化

## 功能

将接口返回或粘贴的 **压缩 JSON 字符串** 格式化为带缩进的多行 JSON，便于阅读与排查。

## 使用步骤

1. 从主界面进入 **JSON 格式化**
2. 在输入区粘贴原始 JSON
3. 执行格式化，右侧或下方显示结果
4. 可复制格式化后的文本

## 说明

- 输入必须是合法 JSON（对象、数组或合法 JSON 片段）
- 非法 JSON 会提示格式错误
